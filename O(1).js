// O(1) means constant in
// suppose you are find a item in array when you get that, thats it done now we dont care of others

const numbers = [1, 2, 3, 4, 5];

const getNumber = (arr, index) => {
  console.log(arr[index]);
};

getNumber(numbers, 0);
